const multi = (a,b) => a * b; //Declaramos una función "CONST"(la referencia a la función no puede ser cambiada) "ARROW" (Más simple y concisa).
    



console.log(multi(324234,47)) //Imprimimos la función con sus argumentos.
