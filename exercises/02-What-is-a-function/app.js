function sum(number1,number2) { //Definimos una función y sus parámetros.
    return number1 + number2; //Especifícamos la operación y retornamos el resultado.
}

sum(3445324, 53454423); //Llamamos a la función y especificamos los argumentos.
let superduper = sum(3445324, 53454423); //Guardamos el resultado en una variable.
console.log(superduper); //Imprimimos el resultado llamando a la variable.